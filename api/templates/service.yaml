apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: api
  type: ClusterIP
  ports:
  - port: {{ .Values.global.config.APP_PORT | default 3030 | int  }}
    targetPort: http
    protocol: TCP
