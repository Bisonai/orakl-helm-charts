apiVersion: v1
kind: Service
metadata:
  name: orakl-monitor-api
spec:
  type: ClusterIP
  ports:
  - port: 8888
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: orakl-monitor-api
    app.kubernetes.io/instance: orakl-monitor-api
