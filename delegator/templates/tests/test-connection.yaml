delegatorVersion: v1
kind: Pod
metadata:
  name: "{{ include "orakl-delegator.fullname" . }}-test-connection"
  labels:
    {{- include "orakl-delegator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orakl-delegator.fullname" . }}']
  restartPolicy: Never
