{{- if .Values.listener.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "orakl-vrf.fullname" . }}-listener
  labels:
    {{- include "orakl-vrf.labels.listener" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "orakl-vrf.fullname" . }}
  minReplicas: {{ .Values.listener.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.listener.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.listener.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.listener.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.listener.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.listener.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
