apiVersion: v1
kind: Service
metadata:
  name: { { .Values.deployment.name } }
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - port: 8090
      targetPort: http
      protocol: TCP
  selector:
    app: { { .Values.deployment.name } }
