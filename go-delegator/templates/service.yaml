apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deployment.name }}
spec:
  # type: ClusterIP
  ports:
  - port: 5050
    targetPort: http
    protocol: TCP
  type: LoadBalancer
  loadBalancerIP: 34.87.152.175
  selector:
    app: {{ .Values.deployment.name }}
