apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.deployment.name }}
spec:
  ports:
    - port: 5050
      targetPort: http
      protocol: TCP
  externalTrafficPolicy: Local      
  type: ClusterIP
  selector:
    app: {{ .Values.deployment.name }}
