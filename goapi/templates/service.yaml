apiVersion: v1
kind: Service
metadata:
  name: orakl-goapi
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: http
      protocol: TCP
  selector:
    app: {{ .Values.deployment.name }}
