apiVersion: v1
kind: Service
metadata:
  name: {{ include "mbxnft-backend.fullname" . }}
  labels:
    {{- include "mbxnft-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mbxnft-backend.selectorLabels" . | nindent 4 }}
