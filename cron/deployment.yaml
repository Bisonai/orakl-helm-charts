apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: logrotate
spec:
  schedule: "* * * * *" # Runs every hour
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: cron
              image: ubuntu:20.04
              command: ["sh", "-c", "apt-get update && apt-get install -y logrotate && logrotate -v /etc/logrotate.conf"]
              volumeMounts:
                - name: logrotate-config
                  mountPath: /etc/logrotate.conf
                  subPath: logrotate.conf
                - name: orakl-log-storage
                  mountPath: /app/log
          restartPolicy: OnFailure
          volumes:
            - name: logrotate-config
              configMap:
                name: logrotate-conf
            - name: orakl-log-storage
              hostPath:
                path: /log/
                type: DirectoryOrCreate
