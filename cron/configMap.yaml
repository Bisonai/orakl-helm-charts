apiVersion: v1
kind: ConfigMap
metadata:
  name: logrotate-conf
data:
  logrotate.conf: |
    /app/log/*.log {
      size 10K
      dateext
      missingok
      rotate 15
      compress
      create 0644 root root
    }
